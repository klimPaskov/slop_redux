# Ireland focus tree event integration matrix

Events are required glue between focuses, decisions, BOP bands, mechanics, and country package changes.

| Family | Focus or decision source | Event role | Failure or crisis link | Cleanup rule |
| --- | --- | --- | --- | --- |
| `opening_constitutional_trunk` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `historical_sovereign_neutrality` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `constitutional_opposition_commonwealth` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `labour_social_republic` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `blueshirt_corporatist` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `ira_underground_republic` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `industry_and_supply` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `military_and_emergency_defence` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `diplomacy_and_foreign_access` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `northern_settlement` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `hidden_path_suite` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `bop_threshold_and_recovery` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `formables_and_late_game` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
| `ai_runtime_cleanup` | route focus family or decision family of same name | opens first visible event and later follow ups | mission failure or BOP threshold events can interrupt or redirect branch | cleanup hidden event removes stale flags and missions |
