# Slop Redux

Local Hearts of Iron IV mod scaffold.

Launcher files:
- `descriptor.mod` inside this folder
- `../slop_redux.mod` in the HOI4 `mod` directory

Both point to:
`C:/Users/klimp/OneDrive/Documents/Paradox Interactive/Hearts of Iron IV/mod/slop_redux`
